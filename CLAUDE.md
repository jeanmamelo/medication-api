# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in
this repository. The full guide is kept in `AGENTS.md` so every agentic tool reads the
same instructions instead of two documents drifting apart.

@AGENTS.md
